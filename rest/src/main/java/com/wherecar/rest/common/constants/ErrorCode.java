package com.wherecar.rest.common.constants;

public enum ErrorCode {
    ACCESS_TOKEN_EXPIRED,
    INVALID_TOKEN,
    REFRESH_TOKEN_EXPIRED,
    UNAUTHORIZED,
    FORBIDDEN,
    INTERNAL_SERVER_ERROR
}
